# Base URL of your NL->SQL API Gateway (without trailing /ask)
# If your HTTP API uses the $default stage (as in this project), do NOT append a stage suffix.
# Example (default stage): https://qpbhjn080e.execute-api.us-east-1.amazonaws.com
# Example (explicit stage): https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/prod
NEXT_PUBLIC_API_URL=

# Optional: brand name for the header
NEXT_PUBLIC_APP_NAME=PubLog AI

# AWS + DynamoDB (server-side) — set these in your hosting environment to enable persistence
AWS_REGION=us-east-1
DDB_TABLE_SAVED=publog_chat_saved
DDB_TABLE_HISTORY=publog_chat_history
